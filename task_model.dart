class Task {
  String
      title; //we declare 2 variables, a string to store the title of the task

  bool complete; //and a bool to display if the task is complete or not

  Task(this.title,
      this.complete); //  we now declare a constructor for both these variables
}
